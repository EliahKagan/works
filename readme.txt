Each stanza in works.txt identifies one or more works, typically by the same
author, that are valuable and that are extremely closely related to each other.
Different works by the same author will typically appear in separate stanzas.
Each stanza is intended as a single recommendation.
